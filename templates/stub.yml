---
meta:
  environment: NAME
  dns_root: microbosh
  network_name: docker_server1
  security_groups:
    - docker_server
  dns: ~

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: docker-server
    version: latest
